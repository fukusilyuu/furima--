<%= turbo_stream.replace dom_id(@user, :follow_button) do %>
  <%= render "relationships/button", user: @user %>
<% end %>

<%= turbo_stream.replace dom_id(@user, :follower_count) do %>
  <%= render partial: "relationships/follow_counts", locals: { user: @user } %>
<% end %>

<%= turbo_stream.replace "notification_bell",
  partial: "shared/header" %>

