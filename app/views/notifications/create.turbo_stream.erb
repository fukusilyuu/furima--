<%= turbo_stream.prepend "notifications",
    partial: "notifications/notification",
    locals: { notification: @user.passive_notifications.last } %>
<%= turbo_stream.replace "notification_bell" do %>
  <%= render "notifications/bell" %>
<% end %>
